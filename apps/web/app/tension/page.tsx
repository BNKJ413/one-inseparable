"use client";
export default function Tension() {
  return (
    <main>
      <h2>We had tension today</h2>
      <div className="card">
        <p className="small">
          MVP placeholder: this screen will guide repair steps (soft start, ownership, reset) with optional Scripture,
          and it must never show paywalls during conflict flows.
        </p>
        <a className="btn" href="/today">Back</a>
      </div>
    </main>
  );
}
